import { storage } from "..";

async function getFileDownloadURL(bucketName: any, fileName: any) {
    // // Get a reference to the file
    // // const fileRef = storage.ref().child(bucketName).child(fileName);
  
    // // Get the download URL for the file
    // const downloadURL = await fileRef.getDownloadURL();
  
    // return downloadURL;
  }